

20260823 agent cpu 内存 优化
```

  生产基线：

  - CPU：60 秒消耗 1.76 CPU 秒，约占单核 2.93%。
  - Go 存活堆：约 5.3 MiB，暂未发现泄漏迹象。
  - 分配速率：约 207.9 MiB / 104 秒，即约 2.0 MiB/s。
  - LZW 与 Gossip UDP 占主要分配；远端元数据处理还触发了不必要的 SQLite 查询。

  已优化：

  - internal/cluster/mesh.go:478 缓存本机 node/boot 身份，移除远端消息处理中的完整 Snapshot()。预计直接移除约 19% 的采样 CPU 调用链。
  - internal/cluster/mesh.go:103 默认关闭 memberlist LZW 压缩，预计整个集群升级后减少约 43% 的临时分配。
  - 新增 gossip.compression 配置，可在带宽优先时重新开启，见 docs/conf/agent.yaml:38。
  - 添加回归测试，确保远端身份检查不再读取完整摘要。

  验证通过：

  - Cluster、Agent 配置测试全部通过。
  - Agent 完整集成测试通过，耗时 339.9s。
  - 全仓库编译检查和 Agent 构建通过。
  - 原始 profile 保存在 /tmp/procmesh-pprof.DKlJXZ/。

  下一步需要部署当前代码，然后通知我再次采样，我会做严格的前后差分。另请尽快关闭公网 16060 或限制来源 IP；pprof 当前可被公网直接访问，会暴露进程内部信息。
```

```
核心改动：

  - internal/agent/store_health.go:13 新增独立健康监控模块：
      - 15 秒执行 quick_check(1)
      - 10 分钟执行完整 integrity_check
      - SQLite 查询结果缓存，并发读取安全

  - internal/agent/run.go:185 启动时仍执行完整检查，并接入后台监控。
  - readiness 每次只同步读取 16 字节文件头，保留数据库被外部覆盖后的即时 503 保护。
  - internal/store/store.go:155 新增 QuickCheck。
  - 完整检查失败只能由完整检查成功清除，避免较弱检查误报恢复。

  验证结果：

  - go test ./... 全部通过，Agent 验收套件耗时 334 秒。
  - 相关测试通过 -race。
  - go vet ./internal/agent ./internal/store 通过。
  - os.ReadFile(layout.Store) 和请求路径中的完整 IntegrityCheck 已移除。
```